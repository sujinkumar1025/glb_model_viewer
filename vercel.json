{
    "builds": [
      {
        "src": "vite.config.js",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "dist"
        }
      }
    ],
    "ignoreCommand": "echo 'Skipping backend folder during deploy'",
    "rewrites": [
      {
        "source": "/api/(.*)",
        "destination": "https://glb-model-viewer-1.onrender.com/api/$1"
      }
    ]
  }
  