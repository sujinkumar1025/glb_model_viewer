jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      VERCEL_DEPLOYMENT_URL: https://glb-model-viewer.vercel.app/
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Echo the deployment URL
        run: echo "Deployed at $VERCEL_DEPLOYMENT_URL"
